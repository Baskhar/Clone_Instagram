import 'package:flutter/material.dart';



class PaginaLoja extends StatelessWidget {
  const PaginaLoja({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Pagina Loja'),
    );
  }
}
