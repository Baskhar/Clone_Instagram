import 'package:flutter/material.dart';

class PaginaProcura extends StatelessWidget {
  const PaginaProcura({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Pagina Procura'),
    );
  }
}
