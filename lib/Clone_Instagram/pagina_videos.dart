import 'package:flutter/material.dart';


class PaginaVideos extends StatelessWidget {
  const PaginaVideos({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Pagina Vídeos'),
    );
  }
}
